# Optional OCR / document-extraction extras for the zero-cost data layer.
# Never required by the Streamlit runtime; used in GitHub Actions or locally.
# System dependency for OCR: tesseract-ocr (apt-get install tesseract-ocr
# tesseract-ocr-spa on ubuntu runners).
pdfplumber>=0.11
pytesseract>=0.3.10
opencv-python-headless>=4.9
Pillow>=10.0
