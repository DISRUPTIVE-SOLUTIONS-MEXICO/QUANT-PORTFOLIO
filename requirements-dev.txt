# Developer / CI extras for the Quant Portfolio-Kaizen app.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Static analysis & security linters
ruff>=0.5
bandit>=1.7
pip-audit>=2.7
mypy==2.1.0
pandas-stubs==3.0.3.260530

# Cross-check / research extras (never required by the Streamlit runtime)
arch>=6.3

# Test infrastructure
pytest>=8.0
pytest-cov>=5.0
